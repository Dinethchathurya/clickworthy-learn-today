services:
  clickworthy:
    build: .
    image: clickworthy:latest
    container_name: clickworthy
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: "3000"
      HOST: "0.0.0.0"
